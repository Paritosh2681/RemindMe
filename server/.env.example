# Server environment variables
PORT=3001
JWT_SECRET=change-this-to-a-long-random-string
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_EMAIL=mailto:you@example.com

# Run `npm run generate-vapid` to get VAPID keys
